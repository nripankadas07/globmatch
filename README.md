# globmatch

Zero-dependency fnmatch-compatible Python glob matcher with extglob, POSIX classes, and `**` globstar.

*This README is a placeholder; the full README and source files are being uploaded.*
